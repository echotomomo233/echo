# 实现一个函数 is_prime：它接受一个正的整数参数 n，返回 n 是否是一个素数。素数指能整除它的数只有 1 和它自己，且不包括 1 的数。
# input:一个正整数 n
# output:True 或 False

def is_prime(n): 
    for a in range(2,n):
        if n % a == 0:
            return False
        return True

    if n == 1:
        return False
    if n == 2:
        return True

n = int(input())
print(is_prime(n))
